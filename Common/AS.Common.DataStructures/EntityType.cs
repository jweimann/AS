namespace AS.Common
{
    /// <summary>
    /// Not yet implemented, will be a type per client entity unless I switch this to a DB driven system
    /// where the Entity packages include a URI
    /// 
    /// Might just use UnityClientActorType instead of this too... need to decide..
    /// </summary>
    public enum EntityType
    {
        Asteroid,
        Ship,
    }
}
